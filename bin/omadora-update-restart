#!/bin/bash

if [ "$(uname -r | sed 's/-arch/\.arch/')" != "$(pacman -Q linux | awk '{print $2}')" ]; then
  gum confirm "New Linux kernel requires reboot. Ready?" && omadora-state clear re*-required && sudo reboot now
elif [ -f "$HOME/.local/state/omadora/reboot-required" ]; then
  gum confirm "Updates require reboot. Ready?" && omadora-state clear re*-required && sudo reboot now
elif [ -f "$HOME/.local/state/omadora/relaunch-required" ]; then
  gum confirm "Updates require Hyprland relaunch. Ready?" && omadora-state clear relaunch-required && uwsm stop
fi
