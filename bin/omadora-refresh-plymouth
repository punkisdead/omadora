#!/bin/bash

if [[ "$1" == "-y" ]] || gum confirm "Refresh Plymouth theme? This will replace your current startup screen with Omadora defaults."; then
  sudo cp ~/.local/share/omadora/default/plymouth/* /usr/share/plymouth/themes/omadora/
  sudo plymouth-set-default-theme -R omadora
fi
